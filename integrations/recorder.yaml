---
recorder:
  db_url: !secret maria_db_url
  purge_keep_days: 14
  exclude:
    entities:
      - sensor.time
      - sensor.heating_boost_end_time
      - sun.sun
    domains:
      - automation
      - camera
      - media_player
      - updater
      # - group
      # - scene
      # - script
